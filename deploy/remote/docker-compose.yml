# vault-cortex — remote quickstart (Obsidian Sync)
#
# Run vault-cortex on a VPS with Obsidian Sync for remote access from any device.
# One container: the :remote image bundles Obsidian Sync and the MCP server
# under s6-overlay supervision.
#
# 1. cp .env.example .env   (then fill in required values)
# 2. docker compose up -d
# 3. Connect your MCP client to http://<your-server>:8000/mcp
#
# Compose is optional — the same container runs with plain `docker run`
# (or podman/nerdctl); see the README for the one-liner.
#
# Full docs: https://github.com/aliasunder/vault-cortex

name: vault-cortex

services:
  vault-cortex:
    image: ghcr.io/aliasunder/vault-cortex:remote
    container_name: vault-cortex
    hostname: ${DEVICE_NAME:-vault-cortex}
    restart: unless-stopped
    environment:
      # --- Obsidian Sync (s6 init chain + sync service) ---
      OBSIDIAN_AUTH_TOKEN: ${OBSIDIAN_AUTH_TOKEN:-}
      VAULT_NAME: "${VAULT_NAME:?Set VAULT_NAME to your Obsidian vault name (case-sensitive)}"
      VAULT_PASSWORD: ${VAULT_PASSWORD:-}
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      DEVICE_NAME: ${DEVICE_NAME:-vault-cortex}
      CONFLICT_STRATEGY: ${CONFLICT_STRATEGY:-merge}
      SYNC_MODE: ${SYNC_MODE:-bidirectional}
      SYNC_EXCLUDED_FOLDERS: ${SYNC_EXCLUDED_FOLDERS:-}
      SYNC_FILE_TYPES: ${SYNC_FILE_TYPES:-}
      # Obsidian config-category sync (default: core plugin data, so the
      # vault's daily-notes settings reach the server). "none" disables;
      # the category values are listed in .env.example.
      SYNC_CONFIGS: ${SYNC_CONFIGS:-core-plugin-data,community-plugin-data}
      # --- MCP server ---
      PORT: "8000"
      HOST: "0.0.0.0"
      VAULT_PATH: /vault
      INDEX_DB_PATH: /data/index.db
      MCP_AUTH_TOKEN: "${MCP_AUTH_TOKEN:?Set MCP_AUTH_TOKEN in .env — see .env.example}"
      PUBLIC_URL: "${PUBLIC_URL:?Set PUBLIC_URL to your server's public URL}"
      # Client-IP trust for rate limiting (see .env.example): the defaults
      # are safe with nothing in front of the server. Set TRUST_PROXY_HOPS=1
      # when a proxy or tunnel sits in front; TRUST_FORWARDED_HOPS=1 only
      # when the proxy reports visitor IPs in the Forwarded header (e.g. AWS
      # API Gateway).
      TRUST_PROXY_HOPS: ${TRUST_PROXY_HOPS:-0}
      TRUST_FORWARDED_HOPS: ${TRUST_FORWARDED_HOPS:-0}
      EMBEDDING_ENABLED: ${EMBEDDING_ENABLED:-true}
      RERANK_MODE: ${RERANK_MODE:-blended}
      MEMORY_ENABLED: ${MEMORY_ENABLED:-true}
      FILE_TOOLS_ENABLED: ${FILE_TOOLS_ENABLED:-true}
      READONLY_MODE: ${READONLY_MODE:-false}
      DISABLED_TOOLS: ${DISABLED_TOOLS:-}
      MEMORY_DIR: ${MEMORY_DIR:-About Me}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      LOG_DIR: ${LOG_DIR:-/data/logs}
      LOG_RETENTION_DAYS: ${LOG_RETENTION_DAYS:-90}
      TZ: ${TZ:-UTC}
      WINDOWS_MODE: ${WINDOWS_MODE:-false}
      MAX_FILE_BYTES: ${MAX_FILE_BYTES:-52428800}
      MAX_IMAGE_OUTPUT_BYTES: ${MAX_IMAGE_OUTPUT_BYTES:-49152}
      MAX_PDF_RENDER_PAGES: ${MAX_PDF_RENDER_PAGES:-5}
      # Optional overrides. When unset, the server applies smart defaults
      # (<MEMORY_DIR>/<DAILY_NOTES_FOLDER> below are the resolved values, defaults "About Me" / "Daily Notes"):
      #   PROTECTED_PATHS           default: "<MEMORY_DIR>, <DAILY_NOTES_FOLDER>" (blocked from vault_delete_note)
      #   ORPHAN_EXCLUDE_FOLDERS    default: "<DAILY_NOTES_FOLDER>, Templates, <MEMORY_DIR>" (excluded from vault_find_orphans)
      #   SERVICE_DOCUMENTATION_URL default: https://github.com/aliasunder/vault-cortex
      #   DAILY_NOTES_FOLDER        default: read from the vault's .obsidian/daily-notes.json, else "Daily Notes"
      #   DAILY_NOTES_FORMAT        default: read from the vault's .obsidian/daily-notes.json, else "YYYY-MM-DD"
      PROTECTED_PATHS: ${PROTECTED_PATHS:-}
      ORPHAN_EXCLUDE_FOLDERS: ${ORPHAN_EXCLUDE_FOLDERS:-}
      SERVICE_DOCUMENTATION_URL: ${SERVICE_DOCUMENTATION_URL:-}
      DAILY_NOTES_FOLDER: ${DAILY_NOTES_FOLDER:-}
      DAILY_NOTES_FORMAT: ${DAILY_NOTES_FORMAT:-}
    volumes:
      - vault_data:/vault
      - mcp_data:/data
      - obsidian_config:/home/obsidian/.config
    ports:
      - "0.0.0.0:${PORT:-8000}:8000"
    # start_period covers the s6 init chain (Obsidian login + sync-setup +
    # first vault sync, run to completion when it succeeds) that runs before
    # the MCP server boots.
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:8000/healthz').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 180s
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }

volumes:
  vault_data:
  mcp_data:
  obsidian_config:
